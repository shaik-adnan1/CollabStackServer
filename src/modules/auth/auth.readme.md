# Docs For AuTH Requirements

```
# Flows

- Signup → returns access token + sets refresh cookie.
- Login → returns access token + sets refresh cookie.
- Refresh → rotates refresh cookie, returns new access token.
- Logout → revokes/clears refresh cookie.
- Me → returns current user profile (requires access token).

```


